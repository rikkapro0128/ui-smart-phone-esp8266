import { memo } from 'react';
import { View, Text } from 'react-native';

function Setting() {
  return (
    <View>
      <Text>Tab Setting For App!</Text>
    </View>
  );
}

export default memo(Setting);
